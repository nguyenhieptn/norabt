# Runtime dependencies actually imported under Agent/ (verified by grepping
# import statements, not guessed). Everything else used in this tree
# (dataclasses, enum, pathlib, statistics, hashlib, ...) is stdlib.

# MCP SDK: agent_server.py is written against the 2.x API
# (mcp.server.mcpserver.MCPServer, renamed from FastMCP in 1.x).
mcp>=2.2,<3

# Schema/validation layer for every Result/Assessment/Decision model across
# Logic 1-3 (pydantic.BaseModel, Field, model_validator, field_validator).
pydantic>=2.5,<3

# Monte Carlo bootstrap (mcp/analytics/simulation/monte_carlo.py) and
# assorted numeric feature extraction across market/ and mcp/analytics/.
numpy>=1.26,<3

# infra/health.py talks to Redis via the asyncio client (redis.asyncio),
# which needs redis-py>=4.2; installed environment runs 8.x.
redis>=5.0,<9

# Test suite (Agent/test/): not imported by application code, needed to run
# `pytest Agent/test`.
pytest>=8.0,<9
